[project]
name = "blue-team-deserialization"
version = "0.1.0"
description = "Blue Team defense and detection pipeline for LangChain deserialization (CVE-2025-68664)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "pytest>=8.3.0",
    "uvicorn>=0.30.0",
]

[dependency-groups]
dev = [
    "ruff>=0.8.0",
]
