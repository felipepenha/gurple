[project]
name = "red-team-deserialization"
version = "0.1.0"
description = "Red Team exploitation client for LangChain deserialization (CVE-2025-68664)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio-client>=1.0.0",
    "httpx>=0.27.0",
]

[dependency-groups]
dev = [
    "ruff>=0.8.0",
]
